plugins {
    id 'nf-schema@2.7.2'
}

params {
    suffix = '_qc'
    capstone_user_label = "${System.getenv('USER') ?: 'user'}"
    capstone_queue_label = {
        if (params.suffix == '_qc') {
            return 'short'
        } else {
            return 'debug'
        }
    }.call()
}

validation {
    ignoreParams = [
        'suffix',
        'capstone_user_label',
        'capstone_queue_label'
    ]
}

profiles {
    test {
        process {
            executor = 'local'
        }
    }
}
